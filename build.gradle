plugins {
    id 'java'
    id('maven-publish')
    id('java-library')
}


group 'io.spielo'
version '0.2'

repositories {
    mavenCentral()
}
sourceSets {
    main {
        java {
            srcDirs = ['src']
        }
    }
}
dependencies {
    compile 'junit:junit:4.12'
}
publishing {
    repositories {
        maven {
            name = "GitHubPackages"
            url = uri("https://maven.pkg.github.com/spielo-io/messages")
            credentials {
                username = "lukesaltweather"
                password = "3367da0a447ed0f3cccec00de0976e515ecf11d7"
            }
        }
    }
    publications {
        gpr(MavenPublication) {
            groupId 'io.spielo'
            artifactId 'messages'
            from(components.java)
        }
    }
}